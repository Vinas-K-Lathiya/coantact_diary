import 'package:flutter/material.dart';

ThemeData lighttheme =
    ThemeData(brightness: Brightness.light, colorSchemeSeed: Colors.black);
ThemeData darktheme =
    ThemeData(brightness: Brightness.dark, colorSchemeSeed: Colors.amber);
